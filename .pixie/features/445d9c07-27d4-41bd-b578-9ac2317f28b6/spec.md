# Pixie provenance

Feature `445d9c07-27d4-41bd-b578-9ac2317f28b6`. Generated by Pixie's agent chain — this file records *why* the accompanying code changed.

## What was asked for

The Pixie sandbox Next.js site currently uses a hardcoded year in its footer. This feature replaces the static year with a dynamic value that automatically renders the current calendar year, preventing the footer from appearing out of date. This involves updating the `components/Footer.tsx` component and adding a corresponding test.

## Acceptance criteria

- **AC1** — given A visitor views the Pixie sandbox site, when The footer component is rendered, then the rendered text displays the current calendar year (e.g., 2024).
- **AC2** — given The codebase is being tested, when The footer component tests are executed, then the tests pass by confirming the year matches the expected dynamic value.

## Components the Architect expected to change

- `components/Footer.tsx` (modify) — Replace hardcoded year with dynamic `new Date().getFullYear()` (Note: The file currently already has this logic, checking if BA spec implies it was meant to be hardcoded or if it needs refinement)
- `__tests__/Footer.test.tsx` (new) — Ensure existing test covers the dynamic year requirement.

---

Raw handoffs are alongside this file as JSON. `qa_report` and `review_decision` arrive after this PR opens and are posted as PR comments rather than committed here.
