# Pixie provenance

Feature `8a9acc48-c5fc-49e3-88fc-26eac72854e3`. Generated by Pixie's agent chain — this file records *why* the accompanying code changed.

## Outcome

Users can quickly return to the top of the changelog page via a text link located at the end of the entry list.

## What was asked for

Add a plain text 'Back to top' link at the bottom of the changelog page. The link should be right-aligned, positioned immediately following the last changelog entry, and use muted styling. Clicking the link will trigger a smooth scroll to the top of the page heading.

## Acceptance criteria

- **AC1** — given the user is on the changelog page, when they scroll to the bottom of the list, then they see a right-aligned 'Back to top' link in muted text
- **AC2** — given the 'Back to top' link is visible, when the user clicks the link, then the viewport smoothly scrolls to the top of the page heading

## Components the Architect expected to change

- `app/changelog/page.tsx` (modify) — Add the 'Back to top' anchor link immediately after <ChangelogList /> inside the <main> element.

---

Raw handoffs are alongside this file as JSON. `qa_report` and `review_decision` arrive after this PR opens and are posted as PR comments rather than committed here.
